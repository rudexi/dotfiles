# vim:sw=2:sts=2:ts=2:
---
- defaults:
    link:
      relink: true
- clean: ['~']
- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.bashrc: bashrc
    ~/.bash_profile: bash_profile
    ~/.vim: vim
    ~/.vimrc: vimrc
