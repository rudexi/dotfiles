# vim:syntax=dosini
[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto
  ui = true
  pager = true

# Aliases for faster git usage
[include]
  path = ~/.gitconfig.d/aliases.ini

# Not provided by dotbot (user section)
[include]
  path = ~/.gitconfig.d/identity.ini
